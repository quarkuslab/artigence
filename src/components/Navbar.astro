---
---

<div class="nav">
    <div class="logowrapper">
        <img class="logo" src="Group.png" alt="">
        <h3>Artigence</h3> <!-- This will now be directly under the logo -->
    </div>

    <!-- Toggle checkbox for menu -->
    <input type="checkbox" id="menu-toggle" class="menu-toggle">
    <label for="menu-toggle" class="menu-icon">&#9776;</label>

    <div class="link">
        <img src="Vector.png" alt="">
        <img src="🦆 icon _facebook icon_.png" alt="">
        <img src="🦆 icon _instagram icon_.png" alt="">
    </div>
</div>

<style>
    .nav {
        display: flex;
        background: #ffffff;
        width: 95%;
        justify-content: space-between;
        align-items: center;
        padding: 10px 20px;
        position: relative;
    }

    .logowrapper {
        display: flex;
        flex-direction: column; /* Stack logo and text vertically */
        align-items: center; /* Center align items */
        color: rgb(0, 0, 0);
    }

    .logo {
        margin-bottom: 5px; /* Space between logo and text */
        height: 40px; /* Set a specific height for the logo */
    }

    h3 {
        margin: 0;
        font-size: 1.5em; /* Font size for brand text */
        text-align: center; /* Center text */
    }

    .link {
        display: flex;
        gap: 10px; /* Space between icons */
    }

    .link img {
        height: 30px; /* Set height for icons */
    }

    /* Toggle checkbox hidden */
    .menu-toggle {
        display: none; /* Hide the toggle checkbox */
    }

    /* Toggle icon styling */
    .menu-icon {
        display: none; /* Hide the toggle icon by default */
        font-size: 1.5em; /* Font size for the toggle icon */
        cursor: pointer; /* Change cursor to pointer on hover */
    }

    /* Responsive styling for mobile */
    @media (max-width: 600px) {
        .nav {
            overflow: hidden;
            flex-direction: row; /* Arrange items in a row */
            justify-content: space-between; /* Space between items */
        }

        .logowrapper {
            
            justify-content: flex-start; /* Align items to the start */
        }

        /* Hide the link container by default on mobile */
        .link {
            display: flex;
            flex-direction: ; /* Stack icons vertically */
            position: fixed; /* Fix position for sliding menu */
            top: 0; /* Align to top */
            right: -100vw; /* Position offscreen */
            /* Width of the slide-out menu */
            margin-right: 50px;
            margin-top: 10px;
            padding: 20px; /* Space from the top */
            transition: right 0.5s ease; /* Smooth transition */
            gap: 20px; /* Space between icons */

        }

        .menu-icon {
            display: block; /* Show toggle icon on mobile */
            z-index: 0; /* Ensure icon is above other content */
            font-size: 1.8em; /* Increase font size */
            position: relative; /* Relative positioning */
        }

        /* Slide the menu into view when checkbox is checked */
        .menu-toggle:checked ~ .link {
            right: 0; /* Slide in the menu */
        }
    }
</style>
